package sample;

public class Ball extends Movable {
    public Ball(double positionX, double positionY, double speedX, double speedY, String ImageUrl) {
        super(positionX, positionY, speedX, speedY,ImageUrl);
    }
}
